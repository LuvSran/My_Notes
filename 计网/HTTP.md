## 常见的 HTTP 请求方法

| HTTP 方法 | 主要用途 (RESTful 规范) |
| ---- | ---- |
| GET | 读取 (Read) 资源或资源列表。 |
| POST | 创建 (Create)新资源，或向服务器提交数据。 |
| PUT | 完整更新 (Update) 或创建资源。用请求体中的数据完全替换目标资源。 |
| DELETE | 删除 (Delete) 目标资源。 |
| PATCH | 部分更新 (Update) 资源。仅发送需要修改的字段。 |
