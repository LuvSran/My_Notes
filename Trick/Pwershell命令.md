`go run file_name1 file_name2` 运行文件
`go run .` 运行当前目录下所有`.go`文件
`go mod init module_name` 当前目录下初始化一个新的Go模块并指定唯一路径名称
`cd name`: 进入当前目录下的 `name` 文件夹。
`cd ..`: 退回到父目录。
`cd ~` : 回到用户主目录。
`cd \`: 回到当前驱动器的根目录（例如 `C:\`）。