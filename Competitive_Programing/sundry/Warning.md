1. `for(;;)`循环范围写对没有？
2. n\*m大小棋盘上格子左往右上往下依次编号,`(i,j)`编号是`(i-1)*m+j`，不是`(i-1)*n+j`！！！
3. 多测清空没有？最好写Qk()函数！！！
4. 位运算注意加括号，比如`(_x&_y)==0`和`_x&_y==0`结果不一样！
5. 递归过程中，是否有不该修改的全局变量被修改？
6. printf输出`long double`是`%LF`而不是`%lf`
7. `vector`下标是从0开始还是1开始。即逆序循环指针边界，正序循环指针开始对了吗？
 8. `set<>` 中元素为自定义结构体时，结构体小于号重载一定要严格偏序，否则会把不同的元素判为相同而去重
 9. 双指针是否需要写`j=max(i,j)`/`j=min(i,j)`
 10. `fill()`的范围写对没有
eg.
 ```
 for(int i=n,j=n;i>=2;--i){
        j=min(j,i);
        while(j>=1 && S[j]!=T[i]) --j;
        if(j==0) KILL(-1);
        fl[i]=j;
    }
```
11. 初始化答案`ans[]`序列时，是否赋值的是非答案值域内的数字？否则这一位判断是否填过答案可能出错
12. 01BFS，一个状态的答案确定只能在队头出队时候确定，不能在入队时确定