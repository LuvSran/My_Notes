```cpp
#include<bits/stdc++.h>
bool bg_memory;
void Prework();
long long iceil(long long _x,long long _y);
long long ABS(long long _x);
#define KILL(x) return cout<<(x)<<'\n',void()
#define ggg cout << "\n*******************************************\n";
#define bug(_x,_y) cout<< "#"<<_x<<" ==>>  "<< _y<< '\n';
#define KKK(_x) cout<<"QwQ => "<< _x<<'\n';
#define ft first
#define sd second
#define pb  push_back
#define MAX(_x,_y,_z) max(_x,max(_y,_z))
#define MIN(_x,_y,_z) min(_x,min(_y,_z))
#define Size(_x) (int)_x.size()
#define SUM(_x,_y) accumulate(_x,_y,0ll)
#define int long long
using namespace std;
using ll = long long;
using pii = pair<ll,ll>;
using i32 = int32_t;
using i128 = __int128;
const ll INF=0x3f3f3f3f3f3f3f3f;
const ll inf=0x3f3f3f3f;
const int maxn=5*1e5+20;
void solve(){
}
bool en_memory;
signed main(){
#ifdef LOCAL_ACM
    chrono::_V2::system_clock::time_point bg_clock,en_clock;
    bg_clock=chrono::high_resolution_clock::now();
    freopen("input.txt","r",stdin);
    freopen("out.txt","w",stdout);
#endif
/*LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran*/
    ios::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    Prework();
    int _;cin >> _;while(_--)
    solve();
/*LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran LuvSran*/
#ifdef LOCAL_ACM
    en_clock=chrono::high_resolution_clock::now();
    auto c=chrono::duration_cast<chrono::microseconds>(en_clock-bg_clock);
    cout<<"\n    Time:"<<0.001*c.count()<<" ms"<<endl;
    cout  <<"    Memory: "<<1.0*(&en_memory-&bg_memory)/1024.0/1024<<" MB"<<'\n';
#endif
    return 0;
}
void Prework(){
}
ll ABS(ll _x){return _x>=0?_x:-_x;}
ll iceil(ll _x,ll _y){return _x/_y+(_x%_y!=0);}
```